#include <stdlib.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	 int n;

	srand(time));
	n = rand() - RAND_MAX / 2;
if (n > 0)
printf("%d is positive\n", n);
else if (n < 0)
printf("%nd is negative\n", n);
else
printf("%nd is zero\n", n);
return (0);
}

