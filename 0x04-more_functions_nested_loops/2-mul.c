#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply with the first number
 *
 * Return: The result of multiplcation between a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
